sudo: required
services:
  - docker

before_install:
  - docker build -t thanushajakkula/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true thanushajakkula/docker-react npm rum test 
